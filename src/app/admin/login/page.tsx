// src/app/admin/login/page.tsx
export const dynamic = 'force-static'; // torna a rota determinística para o validator
export const metadata = { title: 'Login' };

export default function AdminLoginPage() {
    return null; // placeholder; podes trocar pelo teu UI depois
}
